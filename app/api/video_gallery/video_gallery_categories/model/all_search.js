const model = require("./model")

module.exports = async () => {
    // let data = await model.find().where({status: true});
    // // let data = await model.find().where({ title: { $regex: key, $options: "i" }, });
    // return data;
}